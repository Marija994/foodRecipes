<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/landing.css">
    <link rel="stylesheet" href="/css/main.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript"
        async></script>
    <title>Cooking Recipes</title>
</head>

<body>
    <%- include includes/navigation.ejs %>

    <div class="smallScreenText d-block d-sm-none">
        <div class="sstextcontainer">
            <h1>Cooking Recipes</h1>
            <h4>The most delicious place on the internet!</h4>
        </div>
    </div>
    <div class="carouselcontainer d-none d-sm-block">
        <div class="textCarousel ">
            <h1>Cooking Recipes</h1>
            <h4>The most delicious place on the internet!</h4>
        </div>

        <div id="carouselSlides" class="carousel slide carousel-fade" data-ride="carousel" data-interval="4000">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="/slidesImages/slide1.jpg" class="d-block w-100" alt="...">
                </div>
                <div class="carousel-item">
                    <img src="/slidesImages/slide2.jpg" class="d-block w-100" alt="...">
                </div>
                <div class="carousel-item">
                    <img src="/slidesImages/slide3.jpg" class="d-block w-100" alt="...">
                </div>
            </div>
        </div>
    </div>
    </header>

    <main class="mb-5">

        <h1 class="text-center mt-2 mb-5">Explore Categories</h1>

        <div class="row text-center container-fluid mb-5" style="display:flex; flex-wrap: wrap;">
            <% if(categories.length > 0) { %>
            <% categories.forEach(category => { %>
            <div class="col-md-3 col-sm-6 mb-3">
                <div class="thumbnail">
                    <img src="<%= category.image %>">
                    <div class="caption">
                        <h4><%= category.name %></h4>
                    </div>
                    <p>
                        <a href="/category/<%=category._id%>" class="btn btn-info">Browse</a>
                    </p>
                </div>
            </div>
            <% }); %>
            <% } else { %>
            <h1>No Categories Found!</h1>
            <% } %>
        </div>

    </main>

    <%- include includes/footer.ejs %>